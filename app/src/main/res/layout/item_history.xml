<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="76dp"
    android:layout_marginHorizontal="8dp"
    android:layout_marginVertical="3dp"
    app:cardBackgroundColor="#FF1E1E1E"
    app:cardCornerRadius="8dp"
    app:cardElevation="0dp"
    app:strokeColor="#FF333333"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="4dp"
        android:paddingEnd="8dp">

        <!-- Favorite star button (always visible, left side) -->
        <ImageButton
            android:id="@+id/btn_favorite"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_star_outline"
            android:scaleType="centerInside"
            android:padding="4dp" />

        <!-- Content area -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_vertical"
            android:paddingStart="4dp"
            android:paddingEnd="4dp">

            <!-- Title: timestamp + first 2 Chinese chars -->
            <TextView
                android:id="@+id/tv_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:textColor="#FFDDDDDD"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Preview: Chinese text -->
            <TextView
                android:id="@+id/tv_preview_zh"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textSize="12sp"
                android:textColor="#FFFFEECC"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Preview: Russian text -->
            <TextView
                android:id="@+id/tv_preview_ru"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:textSize="11sp"
                android:textColor="#FF99BBDD"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <!-- Multi-select checkbox (right side, only in multi-select mode) -->
        <CheckBox
            android:id="@+id/cb_select"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:buttonTint="#FFAAAAAA"
            android:visibility="gone" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
